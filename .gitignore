# ----------------------------------------------------
# 1. GENERIERTE BUILD-DATEIEN
# ----------------------------------------------------
# Ignoriert den kompletten Maven/Gradle Output Ordner
/target/

# ----------------------------------------------------
# 2. IDE SPEZIFISCHE DATEIEN (VS Code)
# ----------------------------------------------------
# Ignoriert den gesamten .vscode-Ordner (empfohlen)
.vscode/
# Ignoriert die settings.json, falls sie außerhalb von .vscode liegt (optional)
settings.json
# Alle generierten Class-Files, falls sie nicht im target liegen (oft redundant, aber sicher)
*.class

# ----------------------------------------------------
# 3. RESSCOURCEN (JSONs)
# ----------------------------------------------------
# Ignoriert alle .json-Dateien direkt im resources-Ordner:
/src/main/resources/*.json
# Ignoriert alle .json-Dateien in allen Unterordnern des resources-Ordners (rekursiv):
/src/main/resources/**/*.json